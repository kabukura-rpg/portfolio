# 株クラRPG 公式HP — Codex実装指示書

## 0. この指示書の目的

株クラRPGの各コンテンツ・ゲームへの入口となる、**「RPGの拠点／ギルドホール」風の公式ポータルサイト**を制作する。

サイトの役割は、一般的な企業サイトではなく、以下をまとめた**ポートフォリオ兼ゲームランチャー**とする。

- 株クラRPGの紹介
- 登場人物紹介
- PCゲーム一覧
- スマホゲーム一覧
- 各ゲームへの起動導線
- 今後ゲームやコンテンツを増やせる拡張性

デザインは、参考画像のような**暖色の松明に照らされたファンタジーダンジョン**を基調とする。

> 重要：参考画像をそのまま1枚の背景画像として貼り、透明なリンクを重ねる実装にはしないこと。  
> メニュー、カード、見出し、ボタン、テキストなどはHTML/CSSで実装し、実際に操作できるUIにする。

---

# 1. 完成イメージ

## コンセプト

**「株クラRPGの世界に入るための冒険者ギルド」**

トップページにアクセスすると、石造りのダンジョンまたは城内のホールが表示される。

中央上部に「株クラRPG」のロゴ。

その下に、石造りのアーチ／扉をモチーフにした4つの大きな入口を並べる。

1. 株クラRPGとは
2. 人物紹介
3. PCゲーム
4. スマホゲーム

各入口をクリック／タップすると、それぞれ専用ページに遷移する。

PCゲーム、スマホゲームの各ページではゲーム一覧をカード形式で表示し、ゲームカードの「冒険を始める」を押すとゲームが起動する。

---

# 2. 最優先事項

実装時は以下の順で優先する。

1. **見た瞬間に「RPGの公式サイト」と分かること**
2. **ゲームへの導線が分かりやすいこと**
3. **PCとスマホの両方で操作可能なこと**
4. **ゲーム追加が簡単なこと**
5. **過剰なアニメーションで操作性を損なわないこと**
6. **GitHub Pagesでそのまま公開できること**

---

# 3. 技術構成

## 基本方針

初期版は以下で構築する。

- HTML5
- CSS3
- Vanilla JavaScript
- 外部フレームワーク不要
- npm必須にしない
- GitHub Pages対応
- バックエンド不要

ゲーム本体が別HTMLで存在する場合は、サイト配下のゲームディレクトリへ配置し、リンクで起動する。

## 理由

公式HP自体はコンテンツ量が少なく、React等を導入するほど複雑ではない。

静的サイトにすることで、

- デプロイが簡単
- ゲームHTMLをそのまま置ける
- GitHub Pagesとの相性が良い
- 保守が簡単

というメリットを優先する。

---

# 4. 推奨ディレクトリ構成

```text
/
├─ index.html
├─ about.html
├─ characters.html
├─ games-pc.html
├─ games-mobile.html
│
├─ assets/
│  ├─ css/
│  │  ├─ common.css
│  │  ├─ home.css
│  │  └─ pages.css
│  │
│  ├─ js/
│  │  ├─ common.js
│  │  └─ games.js
│  │
│  ├─ images/
│  │  ├─ logo/
│  │  ├─ characters/
│  │  ├─ backgrounds/
│  │  ├─ games/
│  │  └─ ui/
│  │
│  └─ audio/
│     └─ optional/
│
├─ games/
│  ├─ pc/
│  │  ├─ typing-rpg/
│  │  │  └─ index.html
│  │  └─ ...
│  │
│  └─ mobile/
│     ├─ game-name/
│     │  └─ index.html
│     └─ ...
│
├─ data/
│  ├─ characters.js
│  └─ games.js
│
└─ README.md
```

既存ゲームの構成を壊さないこと。

既存ゲームを移動すると相対パスが壊れる可能性がある場合は、無理に移動せず、ゲームURLを `data/games.js` 側で指定する。

---

# 5. 共通デザインシステム

## カラー

ダンジョンをベースに、以下の色系統を使用する。

```css
--bg-deep: #100c09;
--bg-stone: #211912;
--stone-light: #463527;

--wood-dark: #3a2113;
--wood-main: #6d3d1f;
--wood-light: #a9652e;

--gold: #d7a43b;
--gold-light: #ffd977;

--fire: #ef7b21;
--fire-light: #ffc45c;

--text-main: #f7ead4;
--text-sub: #cbb99f;

--danger: #b94235;
--blue: #315f9c;
```

完全一致でなくてもよい。

重要なのは、

- 黒～焦げ茶
- 石壁
- 木材
- 金
- 松明の橙

を中心に統一すること。

---

# 6. フォント

日本語本文は可読性を優先する。

例：

```css
font-family:
  "Noto Serif JP",
  "Yu Mincho",
  "Hiragino Mincho ProN",
  serif;
```

ゲームUI的な箇所はゴシックでもよい。

ロゴや大型見出しは画像化した専用ロゴを使用してもよい。

ただし、ナビゲーション文字・本文・ボタン文字は画像に焼き込まず、HTMLテキストにする。

---

# 7. トップページ仕様

ファイル：

```text
index.html
```

## 7.1 ファーストビュー

画面上部に大型のヒーローエリアを配置する。

### 構成

```text
┌─────────────────────────────┐
│        松明 / 石壁 / 装飾       │
│                             │
│       [ 株クラRPG ロゴ ]       │
│       さあ、爆益の先へ！       │
│                             │
│      キャラクター4人程度       │
│                             │
└─────────────────────────────┘
```

参考画像のように、ロゴの周囲には以下を配置してよい。

- 松明
- クリスタル
- 宝箱
- コイン
- 剣
- 杖
- 本
- 骨
- スライム
- 投資を連想する上昇チャート
- 王冠
- 為替／コインモチーフ

ただし情報量を増やしすぎて、ナビゲーションが埋もれないようにする。

---

# 8. トップページの4大メニュー

ヒーロー直下に4つの大型メニューを配置する。

PCでは4列。

```text
┌──────────┐
│   📜     │
│ 株クラRPGとは │
└──────────┘

┌──────────┐
│   👥     │
│  人物紹介   │
└──────────┘

┌──────────┐
│   🖥     │
│  PCゲーム  │
└──────────┘

┌──────────┐
│   📱     │
│ スマホゲーム │
└──────────┘
```

実際のデザインでは絵文字をそのまま使う必要はない。

以下のようなRPGアイコンに置き換える。

- 株クラRPGとは → 古びた巻物
- 人物紹介 → パーティ／酒場
- PCゲーム → 剣＋キーボード、宝箱など
- スマホゲーム → 魔法の石板／スマホ

## アーチデザイン

各メニューは**石造りのアーチ型入口**として表現する。

アーチ内部は暗くし、マウスオーバー時に奥から光が差す。

### hover

PC：

- 少し上に浮く
- 枠の金色が明るくなる
- 内部の光が強くなる
- アイコンが少し拡大
- 木製看板がわずかに揺れる

```css
transform: translateY(-4px);
```

程度。

大きく動かしすぎない。

### tap

スマホ：

`:hover` に依存しない。

押した瞬間に、

- 明るくなる
- `scale(0.98)`

程度のフィードバックを入れる。

---

# 9. トップページ下部

4大メニューの下に、短い紹介文を置く。

例：

```text
ようこそ、株クラRPGの世界へ。

ここは株クラの仲間たちが作った、
投資と冒険が交差する小さなゲーム世界。

ゲームで遊ぶもよし。
キャラクターを眺めるもよし。

さあ、好きな扉を選ぼう。
```

RPGのメッセージウィンドウ風にする。

黒い半透明背景＋白枠でもよい。

---

# 10. 共通ヘッダー

トップでは世界観を優先し、ヘッダーは控えめにする。

下層ページでは固定ヘッダーを表示する。

```text
[株クラRPG]   TOP | ABOUT | CHARACTER | PC GAME | MOBILE GAME
```

スマホではハンバーガーメニュー。

ただし一般的なモダンWebサイト風の白いナビバーにはしない。

木製看板／石板／黒半透明パネル等にする。

---

# 11. 「株クラRPGとは」ページ

ファイル：

```text
about.html
```

## 内容

### セクション1：株クラRPGとは

簡単なプロジェクト紹介。

仮文言：

```text
株クラRPGは、
株クラの仲間たちで作っている
小さなゲームプロジェクトです。

投資をテーマにしたもの、
タイピングゲーム、
ちょっと変なミニゲームなど。

「ゲームを作ること」だけではなく、
みんなで何かを作ることそのものを楽しむ場所です。
```

文言は後で差し替え可能にする。

---

## セクション2：世界観

羊皮紙／古文書UI。

```text
王国：
フィナンツェ

冒険者たちは、
資産1億円を目指して冒険を続けている――
```

設定は仮置き可能。

---

## セクション3：プロジェクト情報

カード表示。

例：

```text
ジャンル
Webゲーム / ミニゲーム

テーマ
投資 × RPG × 株クラ

プレイ料金
無料

対応
PC / Smartphone
```

---

# 12. 人物紹介ページ

ファイル：

```text
characters.html
```

## レイアウト

キャラクターカードを並べる。

PC：

```text
2～4列
```

スマホ：

```text
1～2列
```

カード例：

```text
┌────────────────────┐
│   キャラクター画像       │
│                      │
│   にむし               │
│   CLASS: ???           │
│                      │
│   短い紹介文             │
│                      │
│   [ 詳細を見る ]         │
└────────────────────┘
```

カードはRPGの冒険者カード風。

---

## 初期キャラクター

現在の株クラRPGメンバーを想定。

- みっちゃん
- にむし
- ねむぱん
- ポンコツ女王

ただし**人物データはHTMLへ直書きせず、一箇所から変更できる構造**を推奨する。

例：

```js
const characters = [
  {
    id: "nimushi",
    name: "にむし",
    className: "???",
    image: "./assets/images/characters/nimushi.png",
    description: "..."
  }
];
```

---

# 13. キャラクター詳細UI

最初は個別HTMLを大量に作らなくてもよい。

カードをクリックするとモーダルで詳細表示する。

例：

```text
┌────────────────────────┐
│       [立ち絵]             │
│                          │
│ にむし                    │
│ CLASS                     │
│                          │
│ HP      ★★★              │
│ 投資力   ★★★★             │
│ 開発力   ★★★★★            │
│                          │
│ PROFILE                  │
│ テキスト...                │
└────────────────────────┘
```

ステータス値は後から設定する。

---

# 14. PCゲームページ

ファイル：

```text
games-pc.html
```

## ページコンセプト

**「PCゲームの酒場／クエストボード」**

ゲームをクエストとして一覧表示する。

---

## ゲームカード

例：

```text
┌──────────────────────────────┐
│ [ゲームサムネイル]             │
│                              │
│ 株クラRPG                    │
│ 詠唱タイピングバトル            │
│                              │
│ 難易度 ★★★☆☆                 │
│ PLAYER 1                     │
│ KEYBOARD                     │
│                              │
│ [ 冒険を始める ]              │
└──────────────────────────────┘
```

クリックするとゲームを開始。

### ゲーム起動

基本：

```html
<a href="./games/pc/typing-rpg/index.html">
```

同一タブで起動。

必要なら将来的に

```html
target="_blank"
```

へ変更できる構造にする。

---

# 15. スマホゲームページ

ファイル：

```text
games-mobile.html
```

PC版とほぼ同じ。

ただしカードに以下を表示できるようにする。

- タップ操作
- 縦画面推奨
- 横画面推奨
- 対応OSの注記

例：

```text
操作：タップ
推奨：スマートフォン
画面：縦向き
```

---

# 16. ゲームデータ管理

ゲームが増えてもHTMLを書き換え続けなくてよい構造にする。

`data/games.js`

例：

```js
window.KABUKURA_GAMES = [
  {
    id: "typing-rpg",
    title: "株クラRPG",
    subtitle: "詠唱タイピングバトル",
    platform: "pc",
    thumbnail: "./assets/images/games/typing-rpg.jpg",
    url: "./games/pc/typing-rpg/index.html",
    difficulty: 3,
    controls: "Keyboard",
    status: "available"
  },
  {
    id: "coming-soon-01",
    title: "？？？？",
    subtitle: "COMING SOON",
    platform: "mobile",
    thumbnail: "./assets/images/games/coming-soon.jpg",
    url: null,
    difficulty: null,
    controls: "Tap",
    status: "coming-soon"
  }
];
```

`platform` でPC／スマホを振り分ける。

---

# 17. COMING SOON

未公開ゲームを表示できるようにする。

```text
COMING SOON
```

カードは暗くして鍵アイコンを表示。

クリック不可。

```css
opacity: 0.6;
cursor: not-allowed;
```

---

# 18. ゲームカード演出

hover時：

- サムネイルが1.02～1.05倍
- 金枠が発光
- 「冒険を始める」ボタンが明るくなる

カード全体を激しく動かさない。

---

# 19. ページ遷移演出

RPG感を出すため、クリック時に短いフェードアウトを行ってよい。

例：

```text
200～300ms
```

ただし遷移を遅く感じさせない。

例：

```css
.page-leave {
  opacity: 0;
}
```

---

# 20. 背景

## PC

石造りのダンジョン背景。

背景画像を使用する場合、

```css
background-attachment: fixed;
background-size: cover;
```

でもよい。

ただしモバイルSafariでは `background-attachment: fixed` に問題が出る場合があるため、スマホでは解除する。

---

# 21. 松明の演出

CSSで軽い明滅をつける。

```css
@keyframes torchGlow {
  0%, 100% {
    filter: brightness(1);
  }

  50% {
    filter: brightness(1.12);
  }
}
```

周期はランダム感があるように見せてもよい。

ただし激しく点滅させない。

---

# 22. パララックス

ヒーロー部分にごく軽い奥行きをつけてもよい。

レイヤー：

```text
background stone
↓
torch glow
↓
characters
↓
logo
↓
foreground decorations
```

マウス移動で最大5～10px程度。

スマホでは無効でもよい。

---

# 23. 音

初期実装ではBGMを自動再生しない。

ブラウザの自動再生制限とユーザー体験を考慮する。

将来的に、

```text
🔊 SOUND ON
```

ボタンを追加できる構造にはしてよい。

---

# 24. レスポンシブ仕様

## Desktop

目安：

```text
1200px以上
```

4大メニューを4列。

ゲームカード3列程度。

---

## Tablet

```text
768px ～ 1199px
```

メニュー2列。

ゲームカード2列。

---

## Smartphone

```text
767px以下
```

メニュー1列または2列。

キャラクター1～2列。

ゲームカード1列。

---

# 25. スマホトップ画面

PCレイアウトを単純縮小してはいけない。

スマホでは以下の順番に縦並び。

```text
LOGO
↓
キャラクター
↓
株クラRPGとは
↓
人物紹介
↓
PCゲーム
↓
スマホゲーム
↓
メッセージ
↓
Footer
```

タップ領域は最低44px以上。

---

# 26. 最大横幅

本文コンテンツは、

```css
max-width: 1200px;
margin: 0 auto;
```

程度。

ヒーロー背景自体は画面幅いっぱい。

---

# 27. アクセシビリティ

最低限以下を守る。

- 画像に `alt`
- キーボード操作可能
- ボタンはbuttonまたはaを使用
- divのみでクリックUIを作らない
- `:focus-visible` を用意
- hoverだけで情報を出さない
- 背景と文字のコントラストを確保
- `prefers-reduced-motion` 対応

例：

```css
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
}
```

---

# 28. SEO / OGP

各ページに設定。

```html
<title>株クラRPG | 公式サイト</title>

<meta
  name="description"
  content="株クラRPGの公式サイト。ゲーム、キャラクター、世界観を紹介します。"
/>
```

OGP：

```html
<meta property="og:title" content="株クラRPG">
<meta property="og:description" content="さあ、爆益の先へ！">
<meta property="og:image" content="...">
<meta property="og:type" content="website">
```

X用：

```html
<meta name="twitter:card" content="summary_large_image">
```

---

# 29. Footer

ダンジョンの床／石壁に自然に続くデザイン。

最低限：

```text
TOP
株クラRPGとは
人物紹介
PC GAME
MOBILE GAME

X
GitHub

© 株クラRPG
```

リンクURLは仮値でもよいが、一箇所で変更しやすくする。

---

# 30. ホーム画面簡易ワイヤーフレーム

```text
┌─────────────────────────────────────────────┐
│                    HEADER                   │
├─────────────────────────────────────────────┤
│                                             │
│               [ CHARACTERS ]                │
│                                             │
│               ┌─────────┐                   │
│               │株クラRPG │                   │
│               └─────────┘                   │
│              さあ、爆益の先へ！               │
│                                             │
├─────────────────────────────────────────────┤
│                                             │
│  ┌───────┐ ┌───────┐ ┌───────┐ ┌───────┐  │
│  │ ABOUT │ │CHARA  │ │PC GAME│ │MOBILE │  │
│  │  📜   │ │  👥   │ │  ⚔   │ │  📱   │  │
│  └───────┘ └───────┘ └───────┘ └───────┘  │
│                                             │
├─────────────────────────────────────────────┤
│                                             │
│           RPG MESSAGE WINDOW                │
│                                             │
│  ようこそ 株クラRPGの世界へ！                 │
│                                             │
├─────────────────────────────────────────────┤
│                    FOOTER                   │
└─────────────────────────────────────────────┘
```

---

# 31. ゲーム一覧画面ワイヤーフレーム

```text
┌─────────────────────────────────────────────┐
│ [← TOP]                PC GAME              │
├─────────────────────────────────────────────┤
│                                             │
│         ― クエストを選択してください ―         │
│                                             │
│ ┌────────────┐ ┌────────────┐ ┌────────────┐│
│ │ thumbnail  │ │ thumbnail  │ │   LOCK     ││
│ │            │ │            │ │            ││
│ │ GAME 01    │ │ GAME 02    │ │ COMING     ││
│ │ ★★★☆☆      │ │ ★★☆☆☆      │ │ SOON       ││
│ │            │ │            │ │            ││
│ │ [PLAY]     │ │ [PLAY]     │ │            ││
│ └────────────┘ └────────────┘ └────────────┘│
│                                             │
└─────────────────────────────────────────────┘
```

---

# 32. 画像素材について

参考画像は**デザイン方向性の資料**として扱う。

必要素材が足りない場合は、以下の仮画像を用意して実装を進める。

```text
assets/images/placeholders/
```

仮画像であることが分かるようにする。

Codexは勝手に既存キャラクターを別キャラクターへ描き直さない。

ユーザーから正式な画像が渡されたら差し替えられる構造にする。

---

# 33. CSSで作る部分 / 画像を使う部分

## CSS中心

- レイアウト
- ボタン
- カード
- 枠
- 発光
- hover
- フェード
- 木製看板の基本形
- モーダル
- レスポンシブ

## 画像推奨

- キャラクター
- 株クラRPGロゴ
- 大型背景
- 松明
- 宝箱
- クリスタル
- 複雑な装飾
- ゲームサムネイル

---

# 34. 重要：画像にUIを焼き込まない

禁止例：

```text
home.png
```

という完成画面1枚を表示し、

```html
<a style="position:absolute">
```

で透明リンクを置く。

これは禁止。

理由：

- スマホ対応困難
- 文言変更困難
- SEO不可
- アクセシビリティ低下
- 将来ゲーム追加が困難

---

# 35. JavaScript

必要最低限にする。

担当：

- モバイルメニュー
- キャラクターモーダル
- ゲームデータ描画
- 軽いページ遷移演出
- 軽いパララックス

ゲーム本体のJSとは分離する。

---

# 36. 共通部品

可能な範囲でCSSクラスを再利用する。

例：

```text
.rpg-panel
.rpg-title
.wood-sign
.stone-card
.gold-button
.quest-card
.character-card
```

見た目をページごとにバラバラにしない。

---

# 37. ローディング

画像量が増えた場合でも、大げさなローディング画面は不要。

ヒーロー以外：

```html
loading="lazy"
```

を使用。

ヒーロー画像は遅延読み込みしない。

---

# 38. パフォーマンス

目標：

- Lighthouse Performance 80以上を目安
- 不要な巨大JSライブラリを入れない
- WebP / AVIFを利用可能なら使用
- 背景画像を必要以上に巨大にしない
- 画像width/height指定
- CLSを減らす

---

# 39. GitHub Pages

相対パスで動くようにする。

悪い例：

```text
/assets/image.png
```

GitHub Pagesのサブディレクトリ配信では壊れる可能性がある。

基本：

```text
./assets/image.png
../assets/image.png
```

を正しく使用。

既存リポジトリ構成に合わせる。

---

# 40. 既存ゲームとの接続

既にゲームが存在する場合、まずリポジトリ内を確認する。

以下を調査する。

```text
index.html
ゲームディレクトリ
assets
BGM
画像
相対URL
localStorage
```

ゲーム本体を不用意に変更しない。

公式HP追加によってゲームの動作が壊れないことを最優先する。

---

# 41. localStorage

既存ゲームのスコア等が `localStorage` に保存されている場合、

**キー名を変更しないこと。**

ゲームをサブディレクトリへ移動する場合も、同一originで動作することを確認する。

---

# 42. MVPで実装する範囲

まず以下を完成させる。

### Phase 1

- `index.html`
- `about.html`
- `characters.html`
- `games-pc.html`
- `games-mobile.html`
- 共通CSS
- 共通ナビ
- レスポンシブ
- 仮画像
- PCゲームへのリンク

ここで一度完成状態にする。

---

# 43. Phase 2

Phase 1完成後に追加。

- hoverアニメーション
- 松明演出
- キャラクターモーダル
- ページ遷移
- COMING SOON
- パララックス
- OGP

---

# 44. Phase 3

必要な場合のみ。

- BGM
- SOUND切替
- お知らせ
- ランキング
- ゲーム検索
- タグ
- プレイ履歴
- 実績
- ギャラリー
- 設定資料集

Phase 1でこれらを先に作らない。

---

# 45. デザイン上の禁止事項

以下は避ける。

- 普通のSaaSサイトのような白背景
- BootstrapそのままのUI
- Material Designそのまま
- 青い一般的なWebボタン
- 過度なGlassmorphism
- RPG世界観と関係のないネオン
- UI全面を画像にする
- 文字が読めないほど暗くする
- hoverで激しく揺らす
- 自動BGM再生
- スマホでPC版を単純縮小

---

# 46. 世界観のキーワード

CSS・UIデザイン判断時は以下の単語を基準にする。

```text
Fantasy
Dungeon
RPG
Guild
Quest
Stone
Wood
Torch
Treasure
Gold
Adventure
Party
Portfolio
Investment
```

雰囲気：

```text
怖いダンジョン
```

ではなく、

```text
かわいいキャラクターが冒険する、
少しコミカルで親しみやすいダンジョン
```

にする。

---

# 47. サイトを見た人に取ってほしい行動

優先順位：

```text
1. ゲームを見つける
2. ゲームを遊ぶ
3. キャラクターを見る
4. 株クラRPGについて知る
```

したがってゲームへの入口は目立たせる。

---

# 48. PC GAME / MOBILE GAME の色分け

必要なら、世界観を壊さない程度に色を分ける。

例：

```text
PC GAME
赤 / 金

MOBILE GAME
青 / 金
```

参考画像にある左右の旗のような使い方でもよい。

---

# 49. ページタイトル例

```text
株クラRPG
冒険者名鑑
PC QUEST
MOBILE QUEST
株クラRPGとは
```

英語だけにせず、日本語を併記する。

---

# 50. 仮コピー

トップ：

```text
株クラRPG

さあ、爆益の先へ！
```

メッセージ：

```text
ようこそ、株クラRPGの世界へ！

ここからキミたちの冒険が始まる……！
```

PCゲーム：

```text
PC QUEST

挑戦するゲームを選んでください。
```

スマホ：

```text
MOBILE QUEST

指先ひとつで、冒険へ。
```

人物紹介：

```text
ADVENTURERS

個性豊かな仲間たち。
```

---

# 51. 実装時にCodexが最初に行うこと

作業開始時に、いきなりコードを書かず、まずリポジトリを確認する。

確認項目：

1. 現在のファイル構成
2. GitHub Pagesの公開方法
3. 既存 `index.html` の用途
4. 既存ゲームの場所
5. assetパス
6. JavaScript依存
7. CSS依存
8. localStorage
9. OGP
10. Git管理状況

その後、既存ゲームを壊さないサイト構成を決定する。

---

# 52. 既存index.htmlがゲーム本体の場合

既存のルート `index.html` がゲーム本体の場合、勝手に上書きしない。

候補：

```text
/games/typing/index.html
```

へ移動する方法を検討する。

ただし移動による影響を確認すること。

影響が大きい場合は、

```text
/portal/
```

等へ公式HPを配置する案もある。

**既存URLを壊す変更は、勝手に行わない。**

---

# 53. Codexの作業単位

一度に全面改修せず、以下の単位で進める。

```text
STEP 1
現状調査

STEP 2
ページ骨格

STEP 3
共通デザイン

STEP 4
トップページ

STEP 5
人物紹介

STEP 6
ゲーム一覧

STEP 7
既存ゲーム接続

STEP 8
レスポンシブ

STEP 9
アニメーション

STEP 10
動作確認
```

---

# 54. 動作確認

最低限以下で確認。

### Desktop

- Chrome
- Safari

### Smartphone相当

DevTools：

```text
iPhone 14 / 15程度
390px
```

および

```text
360px
```

---

# 55. 確認する操作

- TOP → ABOUT
- TOP → 人物紹介
- TOP → PC GAME
- TOP → MOBILE GAME
- GAME一覧 → ゲーム起動
- 戻る操作
- ハンバーガー
- キャラクターモーダル
- 画面回転
- 360px幅
- 1920px幅

---

# 56. 受け入れ条件

以下をすべて満たしたら初期版完成。

## 必須

- [ ] トップがダンジョンRPG風
- [ ] 株クラRPGロゴが目立つ
- [ ] 4カテゴリが一目で分かる
- [ ] 4カテゴリをクリック／タップできる
- [ ] 各専用ページへ遷移できる
- [ ] 人物カードが表示される
- [ ] PCゲーム一覧が表示される
- [ ] スマホゲーム一覧が表示される
- [ ] 公開済みゲームを起動できる
- [ ] COMING SOONを表示可能
- [ ] PCレイアウトが崩れない
- [ ] 390pxスマホで崩れない
- [ ] キーボード操作可能
- [ ] 既存ゲームが壊れていない
- [ ] GitHub Pagesで動作する

---

# 57. 完成時にCodexが報告する内容

作業完了時には以下を簡潔に報告する。

```text
1. 作成／変更したファイル
2. ページ構成
3. ゲーム追加方法
4. キャラクター追加方法
5. 仮画像の場所
6. 未設定URL
7. 動作確認結果
8. 今後差し替えるべき素材
```

---

# 58. ゲーム追加方法を簡単にする

最終的に、新ゲーム追加は原則として

```js
data/games.js
```

へ1件追加し、

```text
assets/images/games/
```

にサムネイルを入れるだけにする。

HTMLを毎回複製しなくてよい構造を目指す。

---

# 59. 将来拡張

この公式サイトはゲーム数が増える前提で設計する。

将来的に以下を追加できる余地を残す。

```text
NEWS
RANKING
GALLERY
SETTING MATERIAL
DOWNLOAD
ACHIEVEMENT
MEMBER
CREDIT
```

ただし初期実装では作らない。

---

# 60. Codexへの最終指示

以下を守って実装すること。

> 株クラRPGの公式HPを、参考画像の世界観をベースにした「ダンジョン型ゲームポータル」として実装してください。
>
> 一般的なWebサイトではなく、RPGの拠点画面に見えることを重視してください。
>
> トップページには「株クラRPGとは」「人物紹介」「PCゲーム」「スマホゲーム」の4つの大型入口を配置してください。
>
> それぞれクリック／タップで専用ページへ移動できるようにしてください。
>
> PCゲーム／スマホゲームページではゲームをクエストカードとして表示し、公開中ゲームの「冒険を始める」を押すとゲームが起動するようにしてください。
>
> 参考画像の完成画面を1枚の背景として貼り付ける方法は禁止です。UIはHTML/CSSで実装し、キャラクターや背景等だけ画像素材として使用してください。
>
> 既存リポジトリを最初に調査し、現在公開中のゲームURLやlocalStorage、画像、BGM、相対パスを壊さないようにしてください。
>
> GitHub Pagesで動作し、PCとスマホの両方に対応してください。
>
> まずMVPを完成させ、その後で軽いアニメーションや演出を追加してください。
>
> 不明な素材は仮画像で実装を止めずに進め、後から差し替え可能にしてください。
>
> 実装後は、変更ファイル・ゲーム追加方法・未設定箇所・動作確認結果を報告してください。

---

# 61. 参考デザインの核心

最後に、このサイトで最も重要なのは以下。

```text
「公式HPを見る」
```

ではなく、

```text
「株クラRPGの拠点に入る」
```

と感じること。

ユーザーがトップページを開いた時点で、

```text
どの扉から冒険する？
```

と思える画面を目指す。
